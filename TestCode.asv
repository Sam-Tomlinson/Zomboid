zomboid = simpleGameEngine('retro_pack.png',16,16,5);
martialArts = randi(9,1,10)
fightMove = []
runLevel(zomboid,'blankScreen.txt')
for i = 1:10
    checkMartial = martialArts(i)
    fightMove(i) = getKeyboardInput(zomboid)
%     if (fightMove(i)) ~= martialArts(i);
%         break
%     end

end